apiVersion: v1
kind: Service
metadata:
  name: {{ include "truenas-password-changer.fullname" . }}
  labels:
    {{- include "truenas-password-changer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "truenas-password-changer.selectorLabels" . | nindent 4 }}
