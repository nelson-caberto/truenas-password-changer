apiVersion: v1
kind: Service
metadata:
  name: {{ include "truenas-password-manager.fullname" . }}
  labels:
    {{- include "truenas-password-manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "truenas-password-manager.selectorLabels" . | nindent 4 }}
